{
	"scripts": {
		"start": "php -S localhost:8080",
		"build": "cp index.html 404.html",
		"update": "rm -rf ./assets/* && rm -rf .update && git clone git@github.com:fwd/docs.git .update && mv .update/assets/* ./assets && mv .update/index.html ./index.html && rm -rf ./.update"
	}
}